import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.technyks.cameraShareApp',
  appName: 'camera-share-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
